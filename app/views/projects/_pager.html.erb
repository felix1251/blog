<% if pagy.next %>
    <div id="pager" class="min-w-full my-8 flex justify-center">
        <%= link_to(
            "Load more..",
            projects_path(page: pagy.next),
            class: "text-sm text-white hover:scale-105 hover:shadow-md py-2 px-5 bg-main font-medium",
            data: { turbo_frame: "page_handler" }
        ) %>
    </div>
<% end %>